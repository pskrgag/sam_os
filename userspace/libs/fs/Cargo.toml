[package]
name = "fs"
version = "0.1.0"
edition = "2024"

[dependencies]
heapless = "0.9.2"
rtl = { path = "../../../rtl" }
hal = { path = "../../../hal" }
bitmask = { git = "https://github.com/pskrgag/bitmask.git", default-features = false }
libc = { path = "../libc" }
rokio = { path = "../rokio" }
serde = { version = "1", default-features = false  }
postcard = { version = "1.1.3", features = ["alloc"] }
spin = "0.10.0"

[build-dependencies]
ridl = { path = "../../../tools/ridl/" }
