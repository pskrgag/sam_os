type Data = Sequence<U8, 1024>;

struct BlockInfo {
	USize blockCount;
	U16 blockSize;
}

interface BlkDev {
	GetInfo(out BlockInfo info);
	ReadBlock(in U32 blockIdx, out Data data);
	SetBlockSize(in U16 blockSize);
}
